#!/bin/bash

# roseshell:summary=Restart an application by killing it and relaunching via uwsm.
# roseshell:args=<application-name> [application-args...]

pkill -x $1
setsid uwsm-app -- "$@" >/dev/null 2>&1 &
